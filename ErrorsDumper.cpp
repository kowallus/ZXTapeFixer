#include "ErrorsDumper.h"

void ErrorsDumper::logError(unsigned int currentSample, unsigned int destCurrentSample, unsigned int signalCount,
                            unsigned int lastSignalCount, float zero, int bytes) {
/*    memoErr->Lines->Add(
            AnsiString(currentSample) + "; " + AnsiString(destCurrentSample) + ": possible error - " +
            signalCount + " - " + lastSignalCount + "; zero - " + zero + "; bytes - " +
            bytes);*/
}
